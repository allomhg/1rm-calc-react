import { createContext } from "react";

export const WeightContext = createContext([null, null]);
export const RepsContext = createContext([null, null]);
export const InputContext = createContext([0, 0]);